import { ComingSoonPage } from "@/components/storefront/ComingSoonPage";

export const metadata = { title: "Wishlist — NAYAAB" };

export default function Page() {
  return <ComingSoonPage title="Wishlist" note="Save products to your wishlist — coming in Phase 9." />;
}
