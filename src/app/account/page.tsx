import { ComingSoonPage } from "@/components/storefront/ComingSoonPage";

export const metadata = { title: "Account — NAYAAB" };

export default function Page() {
  return <ComingSoonPage title="Account" note="Customer accounts, order history and saved addresses — coming in Phase 3/9." />;
}
