import { ComingSoonPage } from "@/components/storefront/ComingSoonPage";

export const metadata = { title: "Sale — NAYAAB" };

export default function Page() {
  return <ComingSoonPage title="Sale" note="Sale items will appear here once products are marked on sale in Admin." />;
}
