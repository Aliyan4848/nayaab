import { ComingSoonPage } from "@/components/storefront/ComingSoonPage";

export const metadata = { title: "Returns & Exchanges — NAYAAB" };

export default function Page() {
  return <ComingSoonPage title="Returns & Exchanges" note="Our return and exchange policy — to be filled in by NAYAAB before launch." />;
}
